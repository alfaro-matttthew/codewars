function integerToString(number) {
    const myInteger = number;
    const myString = myInteger.toString();
    return myString;
}

const startingNumber = 42;
const theFunction = integerToString(startingNumber);
console.log(theFunction);